============
Installation
============

At the command line::

    $ easy_install cbh_chembl_ws_extension

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv cbh_chembl_ws_extension
    $ pip install cbh_chembl_ws_extension
